# My_First_VHDL_CPU
A simple 4-bit CPU (Processor) designed from scratch in VHDL. This repo documents the step-by-step learning process, including all modules (ALU, FSM, Counter) and their verification testbenches.
